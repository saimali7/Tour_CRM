# =============================================================================
# LOCAL DEVELOPMENT - Environment Variables
# =============================================================================
# This file uses local Docker services instead of cloud providers
# Copy to .env.local: cp .env.local.example .env.local
#
# Legend:
#   [REQUIRED] - Must be set for the app to run
#   [OPTIONAL] - Feature works without it (degrades gracefully)
#   [PRODUCTION] - Required in production, optional in dev

# =============================================================================
# DATABASE [REQUIRED]
# =============================================================================
# Local PostgreSQL via Docker
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tour_platform
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/tour_platform

# Alternative: Use Supabase Cloud (free tier available)
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# DIRECT_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# =============================================================================
# AUTHENTICATION [OPTIONAL IN DEV]
# =============================================================================
# Set to "false" to skip Clerk auth in development (uses mock auth)
# Set to "true" to use Clerk auth (requires valid keys below)
ENABLE_CLERK=false

# Clerk - Get from https://clerk.com (create free account)
# Required if ENABLE_CLERK=true
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME
CLERK_SECRET_KEY=sk_test_REPLACE_ME
CLERK_WEBHOOK_SECRET=whsec_REPLACE_ME
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# =============================================================================
# PAYMENTS [OPTIONAL]
# =============================================================================
# Stripe - Get from https://dashboard.stripe.com/test/apikeys
# Payment features disabled if not configured
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME

# For local webhook testing, use Stripe CLI:
# stripe listen --forward-to localhost:3000/api/webhooks/stripe

# =============================================================================
# EMAIL [OPTIONAL]
# =============================================================================
# Option 1: Local Mailpit via Docker (catches all emails)
# Mailpit Web UI: http://localhost:8025
SMTP_HOST=localhost
SMTP_PORT=1025
EMAIL_FROM=noreply@localhost

# Option 2: Resend (production-quality, has free tier)
# Get from https://resend.com
RESEND_API_KEY=re_REPLACE_ME

# =============================================================================
# CACHE [OPTIONAL]
# =============================================================================
# Local Redis via Docker
REDIS_URL=redis://localhost:6379

# =============================================================================
# STORAGE [OPTIONAL]
# =============================================================================
# Local MinIO via Docker (S3-compatible)
# MinIO Console: http://localhost:9001 (minioadmin/minioadmin)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=tour-images
S3_PUBLIC_URL=http://localhost:9000/tour-images

# =============================================================================
# BACKGROUND JOBS [OPTIONAL]
# =============================================================================
# Inngest - Get from https://inngest.com (has free tier)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# =============================================================================
# APP CONFIGURATION [REQUIRED]
# =============================================================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_WEB_URL=http://localhost:3001

# Security [PRODUCTION REQUIRED]
# Generate with: openssl rand -base64 32
JWT_SECRET=your-32-character-minimum-secret-for-jwt

# =============================================================================
# SMS [OPTIONAL]
# =============================================================================
# Twilio - Get from https://twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# =============================================================================
# MONITORING [OPTIONAL]
# =============================================================================
# Sentry - Get from https://sentry.io
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
